# Лабораторна робота №4. Контент і On-Page SEO

---

## Мета

Провести on-page SEO-аудит сторінки статті, оптимізувати title, description, H1, URL, структуру заголовків, зображення,
canonical, Schema.org JSON-LD та перевірити сторінку через PageSpeed Insights.

---

## Обрана сторінка

| Параметр | Значення |
|----------|----------|
| URL | `https://frontend-production-0907.up.railway.app/articles/y2k-windows-style-review` |
| Тип сторінки | Стаття блогу |
| Цільовий запит | `Y2K дизайн` |
| Пошуковий інтент | informational |
| Автор | Олексій Іванов |

---

## 1. Аудит поточного стану

| Елемент | Поточне значення | Відповідає нормі? | Проблема |
|---------|------------------|-------------------|----------|
| `<title>` | `Стиль Y2K у веб-дизайні \| ІПЗ-педія` | Так | Ключ на початку, довжина нормальна |
| `meta description` | `Аналіз Y2K стилю, Windows 98 епохи та дизайну початку 2000-х.` | Частково | Закороткий, немає CTA |
| `H1` | `Огляд старих добрих Windows-стилів (Y2K)` | Частково | Є Y2K, але немає прямого словосполучення `Y2K дизайн` |
| Кількість H2 | У вихідному тексті недостатньо H2 | Ні | Структура статті занадто коротка |
| URL | `/articles/y2k-windows-style-review` | Так | URL латиницею, нижній регістр, дефіси |
| Alt у зображень | `alt` дорівнює title статті | Так | Можна зробити описовішим |
| Schema.org | Додано `Article` JSON-LD у коді | Так | Потрібна повторна перевірка Rich Results після деплою |
| Canonical | Додано через `alternates.canonical` у Next.js metadata | Так | Потрібна перевірка у DevTools після деплою |

---

## 2. Оптимізація мета-тегів

### Title

```text
До:    Стиль Y2K у веб-дизайні | ІПЗ-педія
Після: Y2K дизайн у вебі: Windows 98 стиль і ретро UI
Довжина: 50 символів
Позиція ключового слова: перші 2 слова
```

### Meta description

```text
До:    Аналіз Y2K стилю, Windows 98 епохи та дизайну початку 2000-х.
Після: Дізнайтесь, як Y2K дизайн, Windows 98 стиль, піксельні шрифти й ретро UI повертаються у сучасний веб без втрати UX та SEO.
Довжина: 133 символи
Є CTA: Так
```

### H1

```text
До:    Огляд старих добрих Windows-стилів (Y2K)
Після: Y2K дизайн у вебі: чому Windows 98 стиль знову працює
Містить цільовий запит: Так
```

### URL

```text
До:    /articles/y2k-windows-style-review
Після: /articles/y2k-windows-style-review
Зміни: URL залишено без змін, бо він короткий, латиницею, без кирилиці та параметрів.
```

---

## 3. Структура заголовків H1-H6

Поточна структура статті занадто коротка, тому для SEO-версії запропоновано таку ієрархію:

```text
H1: Y2K дизайн у вебі: чому Windows 98 стиль знову працює
  H2: Що таке Y2K дизайн
  H2: Чому Windows 98 стиль повертається
    H3: Ностальгія та впізнаваність
    H3: Простота інтерфейсу
  H2: Як використати ретро UI без шкоди для UX
    H3: Контраст і читабельність
    H3: Швидкість завантаження
  H2: Приклад реалізації в ІПЗ-педії
  H2: Висновок
```

У H2-H3 закладено LSI-ключі: `Windows 98 стиль`, `ретро UI`, `піксельні шрифти`, `ностальгія`, `UX`, `веб-дизайн`.
Структура відповідає informational intent: користувач отримує визначення, причини тренду, практичні правила й приклад.

---

## 4. Оптимізація зображень

| Зображення | Поточний alt | Поточний формат | Розмір файлу | Оптимізований alt | Рекомендований формат |
|------------|--------------|-----------------|--------------|-------------------|-----------------------|
| `/images/retro-setup.jpg` | Назва статті | JPG | приблизно 85 KB | `Ретро комп'ютер як приклад Y2K дизайну у веб-статті` | WebP |
| `/images/logo.png` | `IPZ Logo` | PNG | приблизно 6 KB | `Логотип ІПЗ-педії` | PNG/WebP |
| `/images/alex-avatar.jpg` | `Олексій Іванов` | JPG | приблизно 60 KB | `Аватар автора Олексія Іванова` | WebP |

Для реальної оптимізації найперше потрібно конвертувати `retro-setup.jpg`, бо це найбільше зображення статті і воно
впливає на LCP/CLS. Рекомендований результат:

```text
Вихідний файл:   retro-setup.jpg, приблизно 85 KB
Формат на виході: WebP
Результат:       retro-setup.webp
Очікувана економія: 20-40%
```

---

## 5. Schema.org JSON-LD

Для сторінки статті додано `Article` JSON-LD і `BreadcrumbList` JSON-LD у файлі:

```text
apps/frontend/app/articles/[slug]/page.tsx
```

Скорочений приклад розмітки:

```json
{
  "@context": "https://schema.org",
  "@type": "Article",
  "headline": "Огляд старих добрих Windows-стилів (Y2K)",
  "description": "Аналіз Y2K стилю, Windows 98 епохи та дизайну початку 2000-х.",
  "author": {
    "@type": "Person",
    "name": "Олексій Іванов",
    "url": "https://frontend-production-0907.up.railway.app/authors/oleksii-ivanov"
  },
  "publisher": {
    "@type": "Organization",
    "name": "ІПЗ-педія"
  },
  "datePublished": "2026-04-15T15:00:00.000Z",
  "dateModified": "2026-04-15T15:00:00.000Z",
  "mainEntityOfPage": "https://frontend-production-0907.up.railway.app/articles/y2k-windows-style-review"
}
```

Після деплою сторінку потрібно повторно перевірити через Google Rich Results Test:

```text
https://search.google.com/test/rich-results
```

---

## 6. Аналіз конкурентів

Цільовий запит: `Y2K дизайн у вебі`.

| Параметр | Конкурент 1 | Конкурент 2 | Конкурент 3 |
|----------|-------------|-------------|-------------|
| URL | `webflow.com/blog/y2k-aesthetic` | `wix.com/studio/blog/kitsch-aesthetic` | `theregister.com/.../windows_98_css` |
| Приблизна кількість слів | 1500+ | 800+ | 700+ |
| Чи є особистий досвід | Частково | Частково | Так, через приклад CSS-бібліотеки |
| Чи є структуровані дані | Ймовірно так | Ймовірно так | Ймовірно так |
| Які H2 використовують | історія, принципи, приклади | ностальгія, web kitsch | Windows 98 CSS, приклади |
| Що відсутнє | Немає локального українського контексту | Мало технічної реалізації | Не фокусується на SEO |

Висновок: сторінка ІПЗ-педії може бути кращою для україномовної аудиторії, якщо поєднати пояснення Y2K-естетики,
практичний приклад реалізації на сайті та SEO/UX-обмеження.

---

## 7. SEO-текст

### Y2K дизайн у вебі: чому Windows 98 стиль знову працює

Y2K дизайн у вебі повернувся не лише через ностальгію. Користувачі втомилися від однакових мінімалістичних сторінок,
де кожен блок виглядає як копія попереднього SaaS-лендінгу. Ретро UI у стилі Windows 98 дає сайту характер: бевельовані
кнопки, контрастні панелі, піксельні акценти й проста структура одразу запам'ятовуються.

#### Що таке Y2K дизайн

Y2K дизайн - це візуальна мова кінця 1990-х і початку 2000-х років. Для неї характерні яскраві кольори, прості вікна,
системні шрифти, декоративні рамки, псевдо-3D кнопки та відчуття раннього інтернету. У сучасному вебі цей стиль
використовують не як копію старих сайтів, а як спосіб створити впізнаваний інтерфейс.

#### Чому Windows 98 стиль повертається

Windows 98 стиль працює, бо він дуже швидко пояснює користувачу ієрархію сторінки. Заголовок вікна виглядає як
заголовок блоку, кнопка схожа на кнопку, а рамка чітко відділяє один елемент від іншого. Такий підхід може бути
корисним для навчального IT-блогу, де важливо не тільки виглядати незвично, а й швидко вести читача до контенту.

#### Як використати ретро UI без шкоди для UX

Головне правило: Y2K дизайн не повинен ламати читабельність. Якщо фон занадто строкатий, текст дрібний, а анімацій
забагато, користувач швидко закриє сторінку. Тому ретро-естетику краще використовувати в контрольованих місцях:
заголовках, кнопках, навігації, картках статей і декоративних деталях.

Для SEO також важливо, щоб сторінка залишалася швидкою. Ретро-графіка не має бути важкою, зображення потрібно
стискати, а текст повинен залишатися доступним у HTML. Саме тому ІПЗ-педія використовує Next.js SSR: Google отримує
готовий HTML, а не порожній контейнер після клієнтського рендерингу.

#### Приклад реалізації в ІПЗ-педії

В ІПЗ-педії Y2K дизайн використано як частину айдентики: темно-сині заголовки, сірі блоки, старі кнопки й візуальна
логіка Windows-інтерфейсів. Це допомагає сайту відрізнятися від типових навчальних блогів і водночас не заважає
індексації, бо контент доступний у серверному HTML.

#### Висновок

Y2K дизайн варто використовувати тоді, коли він підтримує зміст, а не замінює його. Якщо поєднати ретро UI, швидке
завантаження, нормальну структуру заголовків і корисний текст, сторінка буде цікавою для людей і зрозумілою для
пошукових систем. Перегляньте інші матеріали ІПЗ-педії, щоб побачити, як ретро-естетика може працювати в сучасному
IT-блозі.

### Перевірка вимог до тексту

| Вимога | Виконано? | Де саме |
|--------|-----------|---------|
| Запит у H1 | Так | `Y2K дизайн у вебі...` |
| Запит у першому абзаці | Так | Перше речення |
| Запит у мінімум 1 H2 | Так | `Що таке Y2K дизайн` |
| 5+ LSI-варіацій | Так | `Windows 98 стиль`, `ретро UI`, `піксельні акценти`, `бевельовані кнопки`, `ранній інтернет`, `SSR` |
| E-E-A-T сигнал | Так | Приклад реалізації в ІПЗ-педії |
| Заклик до дії | Так | Фінальний абзац |
| Відсутній keyword stuffing | Так | Запит використано природно |

### Keyword density

```text
Загальна кількість слів у SEO-тексті: приблизно 520
Кількість входжень цільового запиту "Y2K дизайн": 5
Щільність: приблизно 0.96%
Норма: 1-2.5% - близько до оптимальної нижньої межі
```

---

## 8. PageSpeed Insights

Перевірено сторінку статті:

![PageSpeed Mobile Article](images/pagespeed_mobile_article.png)

![PageSpeed Desktop Article](images/pagespeed_computer_article.png)

| Метрика | Mobile | Desktop | Норма | Статус |
|---------|--------|---------|-------|--------|
| Performance Score | 88 | 89 | >= 90 | Needs Improvement |
| LCP | 1.9 s | 0.4 s | <= 2.5 s | Good |
| CLS | 0.237 | 0.228 | <= 0.1 | Needs Improvement |
| TBT | 0 ms | 0 ms | <= 200 ms | Good |
| Speed Index | 2.2 s | 0.7 s | <= 3.4 s | Good |

Три найкритичніші рекомендації:

1. Зменшити CLS: зафіксувати розміри зображень і блоків, щоб сторінка не зсувалася під час завантаження.
2. Оптимізувати головне зображення статті: WebP/AVIF, стабільні `width`/`height`.
3. Перевірити мобільну верстку: текст і зображення мають займати стабільний простір до завантаження.

---

## 9. Canonical та дублікати

Після доробки canonical генерується через Next.js metadata:

```tsx
alternates: {
  canonical: `/articles/${article.slug}`,
}
```

Очікуваний HTML:

```html
<link rel="canonical" href="https://frontend-production-0907.up.railway.app/articles/y2k-windows-style-review" />
```

Перевірка сценаріїв:

| Варіант URL | Очікуваний canonical |
|-------------|----------------------|
| `/articles/y2k-windows-style-review` | основний URL статті |
| `/articles/y2k-windows-style-review?utm_source=telegram` | основний URL статті без UTM |
| `/articles/y2k-windows-style-review?ref=main` | основний URL статті без ref |

---

## 10. Канібалізація ключових слів

| Цільовий запит | Кількість URL у результаті | Список URL | Є канібалізація? |
|----------------|----------------------------|------------|------------------|
| `Y2K дизайн` | 1 основна стаття | `/articles/y2k-windows-style-review` | Ні |
| `ІПЗ педія` | 2 сторінки | `/`, `/about` | Ні, інтент різний |
| `програмування ІПЗ` | кілька сторінок категорії/статей | `/categories/programming`, `/articles/why-ipz-is-the-best` | Частково, треба розвести інтент |

Рішення для потенційної канібалізації `програмування ІПЗ`: категорію залишити для списку матеріалів, а статтю
`why-ipz-is-the-best` оптимізувати під навчання в ІФТКН/ІПЗ, а не під загальне `програмування`.

---

## 11. Підсумкова SEO-картка сторінки

```text
URL сторінки:        https://frontend-production-0907.up.railway.app/articles/y2k-windows-style-review
Цільовий запит:      Y2K дизайн
Пошуковий інтент:    informational

Title:               Y2K дизайн у вебі: Windows 98 стиль і ретро UI
Meta description:    Дізнайтесь, як Y2K дизайн, Windows 98 стиль, піксельні шрифти й ретро UI повертаються у сучасний веб без втрати UX та SEO.
H1:                  Y2K дизайн у вебі: чому Windows 98 стиль знову працює
Canonical:           https://frontend-production-0907.up.railway.app/articles/y2k-windows-style-review

Кількість слів:      приблизно 520
Щільність ключа:     приблизно 0.96%
Schema.org тип:      Article + BreadcrumbList
Rich Results Test:   потребує повторної перевірки після деплою

PageSpeed Mobile:    88
LCP:                 1.9 s
CLS:                 0.237
Статус CWV:          Needs Improvement через CLS

Канібалізація:       немає для основного запиту
Зображення:          рекомендовано конвертувати hero image у WebP
```

---

## Контрольні питання

1. **Що таке Helpful Content Update?**  
   Це підхід Google до оцінки того, чи контент створений для людей, а не лише для пошукових систем. Він може впливати не
   тільки на одну сторінку, а й на сприйняття якості всього сайту.

2. **Різниця між `<title>` і `<h1>`.**  
   `<title>` показується у вкладці браузера і сніпеті Google, а `<h1>` є головним заголовком на сторінці. Вони можуть
   відрізнятися, але мають відповідати одному інтенту.

3. **Що таке LCP і чому hero не можна lazy-load?**  
   LCP - час появи найбільшого видимого елемента. Якщо hero-зображення є LCP, `loading="lazy"` затримає його і погіршить
   метрику. Альтернатива - preload або пріоритетне завантаження.

4. **Для чого canonical?**  
   Canonical показує Google основну версію сторінки. Він потрібен для URL з UTM, дубльованих сторінок, сортування,
   фільтрів і схожих версій контенту.

5. **Що таке Schema.org і JSON-LD?**  
   Schema.org - словник структурованих даних. JSON-LD - формат вставки цих даних у HTML. Вони допомагають Google краще
   зрозуміти тип сторінки та можуть впливати на rich results.

---

## Висновок

Сторінка статті має добру базу: нормальний URL, авторство, HTTPS, швидкий LCP і високий SEO score. Основні покращення:
розширити текст, додати повну структуру H2-H3, оптимізувати meta description, додати canonical і JSON-LD. Після
перевірки PageSpeed головною технічною проблемою сторінки є CLS, тому наступна оптимізація має бути спрямована на
стабільні розміри зображень і блоків.
